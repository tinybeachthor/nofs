[project]
name = "nofs"
version = "0.1.0"
description = ""
authors = [
    {name = "Martin",email = "25009432+tinybeachthor@users.noreply.github.com"}
]
requires-python = ">=3.13"
dependencies = [
    "pyfuse3>=3.3.0",
    "trio>=0.22.0",
]

[project.scripts]
nofs = "nofs.__main__:main"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
